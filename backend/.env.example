# Backend Environment Variables Example
# Copy this file to .env and fill in your actual values.
# The .env file should be in your .gitignore to keep secrets out of version control.

# --- Database ---
# Connection string for your PostgreSQL database.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE_NAME
# Example for local development:
DATABASE_URL="postgresql://your_db_user:your_db_password@localhost:5432/projectrack_db"
# If using the project's current default:
# DATABASE_URL="postgresql://postgres:Sahilgarima2003@localhost:5432/my_db"


# --- Authentication ---
# Secret key for signing and verifying JSON Web Tokens (JWTs).
# This should be a long, random, and strong string.
# Generate a new one for your actual .env file.
# Example: openssl rand -base64 32  (run this in your terminal to get a random string)
JWT_SECRET="your_super_secret_and_long_jwt_key_must_be_changed_in_actual_env"


# --- Server ---
# Port for the backend Express server to listen on.
# Defaults to 3001 if not specified in the code.
# PORT=3001